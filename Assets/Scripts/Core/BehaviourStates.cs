namespace ScalePact.Core
{
    [System.Serializable]
    public enum BehaviourStates
    {
        NO_STATE,
        DEATH_STATE,
        ATTACK_STATE,
        PATROL_STATE,
        CHASE_STATE,
        SUSPICION_STATE,
        IMPACT_STATE,
        count
    }
}